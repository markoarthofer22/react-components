import { Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Select } from '../src/index';

# Select

Select component for everyday usage :)

### Usage and import

You can import it in two ways: - as a standalone component - from the whole package

```jsx
import Select from '@markoarthofer22/react-components.select';
// OR
import { Select } from '@markoarthofer22/react-components';
```

<Canvas withSource={'open'}>
    <Story id='designs-atoms-select--default' />
</Canvas>

You should use `bindingValue` prop to specify to what will Select bind its value. In this example `country` prop in data

<div style={{ marginBottom: '50px' }} />

### API

<ArgsTable of={Select} />
